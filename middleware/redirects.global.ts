export default defineNuxtRouteMiddleware((to, from) => {
  // const url = to.path;
  // if (url.slice(-1, url.length) == "/" && to.path != "/") {
  //   return navigateTo(url.slice(0, -1), { replace: true })
  // }
})